class Solution {
public:
    void reverseString(vector<char>& s) {
       int i,j;
       int n= s.size();
       i=0;
       j=n-1;
       while(i<j)
       {
           swap(s[i],s[j]);
           i++;
           j--;
       } 
    }
};
